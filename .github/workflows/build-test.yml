name: Build Test

on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: gfortran on macos
        run: |
          # make sure gfortran is available
          gfortran --version
